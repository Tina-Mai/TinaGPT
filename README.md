# TinaGPT

finetuning a Llama 3 8B base model on approximately 10,000 tokens from old writing to mimic my writing style.

start by running:

```bash
./setup.sh
```

then run:

```bash
python main.py
```
